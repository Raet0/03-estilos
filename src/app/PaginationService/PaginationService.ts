import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-pagination-service',
  imports: [],
  templateUrl: './PaginationService.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class PaginationService { }
