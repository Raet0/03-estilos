import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-tabla-targeta',
  imports: [],
  templateUrl: './tablaTargeta.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class TablaTargeta { }
